import React, { useEffect } from "react";
import { useParams } from "react-router-dom";

export const Demo = (props: any) => {

    const {} = useParams(); 

    useEffect(() => {
        
    }, [])

    return (
        <div>
            {/* Place your JSX code*/}
        </div>
    )
}